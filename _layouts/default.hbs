{{> header}}
{{> nav}}
<div class="container">
  <div class="jumbotron">
    <h1>{{title}}</h1>
    {{#if excerpt}}{{{excerpt}}}{{/if}}
  </div>
  <article>
    {{{contents}}}
  </article>
</div>
{{> footer}}
